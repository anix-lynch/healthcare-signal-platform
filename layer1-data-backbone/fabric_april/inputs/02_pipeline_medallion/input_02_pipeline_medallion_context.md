# Input 02 — Data Factory pipeline (medallion)

**What:** Fabric pipeline that moves data bronze → silver → gold (can be minimal: one Copy + one Notebook).

**Export:** Replace placeholder with **Download definition** or copy JSON from Fabric when available; save final artifact to `fabric_april/outputs/02_exports/fabric_pipeline_definition.json`.

**Unlocks:** F2 proof (screenshot of successful run and/or JSON export).
