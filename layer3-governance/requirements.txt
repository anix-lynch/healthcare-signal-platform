# Core
google-cloud-aiplatform>=1.60.0
vertexai>=1.60.0
anthropic>=0.39.0
openai>=1.50.0

# RAG + Eval
ragas>=0.2.0
datasets>=3.0.0
chromadb>=0.5.0
langchain>=0.3.0
langchain-google-vertexai>=2.0.0

# Compliance
spacy>=3.7.0
# after install: python -m spacy download en_core_web_sm
# NeMo Guardrails (30% risk on M4 — skip if install fails)
# nemoguardrails>=0.10.0

# Utilities
pydantic>=2.0
python-dotenv>=1.0
requests>=2.32
tqdm>=4.66

# Healthcare-da FastAPI side (already in healthcare_da_src/api/requirements.txt)
# fastapi uvicorn sqlalchemy dbt-bigquery etc. — install via healthcare_da_src/api/
